name = "epstein"
pages_build_output_dir = "."
compatibility_date = "2024-01-01"

# Read-only access to the same D1 database as the API worker, used by
# Pages Functions to server-render crawlable document/sitemap pages.
[[d1_databases]]
binding = "DB"
database_name = "epstein-files-db"
database_id = "ae344bab-8a31-422d-bb07-fc07d5d69189"
